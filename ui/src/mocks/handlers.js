// import { rest } from 'msw'


// ** mock data JSON
// import thread from './data/thread.json'
// import comments from './data/comments.json'
//Set URL to mock
//const baseUrl = process.env.REACT_APP_API_URL || 'http://localhost:9000'

export const handlers = [
    // rest.get(`${baseUrl}/thread`, (req, res, ctx) => { // capture "GET /greeting" requests
    //   return res(ctx.json(thread)) // respond using a mocked JSON body
    // }),

    // rest.get(`${baseUrl}/thread/comments`, (req, res, ctx) => { // capture "GET /greeting" requests
    //   return res(ctx.json(comments)) // respond using a mocked JSON body
    // }),
  ]